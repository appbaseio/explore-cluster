## Project Structure

Env file is at cypress.json (root directory)

See `integrations` folder for the integrations test.

## Flow

Run the tests manually by running the flow locally on Arc dashboard.


## How To Test Locally

`yarn cypress:run`

