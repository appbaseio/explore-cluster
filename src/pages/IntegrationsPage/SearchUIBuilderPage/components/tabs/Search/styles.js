import { Button, Card } from 'antd';
import styled from 'react-emotion';

export const CodeEditorCard = styled(Card)`
	position: relative;
`;

export const CardButton = styled(Button)`
	position: absolute;
	box-sizing: border-box;
	top: 10px;
	right: 10px;
`;
